[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = [".vendor_httpx", ".pytest_tmp", ".piptmp", ".sandboxtest", ".research_astrbot"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
